#! /usr/bin/bash

# special variables
# note: these variables are declared by the shell (BASH)
echo "the exit status of last process: $?"
echo "the name of the bash file: $0"
echo "the process id (PID) of the bash process: $$"
echo "the process id (PID) of the last background process: $!"
echo "the last argument of previous command: $_"
